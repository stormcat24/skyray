syntax = "proto3";
package endpoint;

message Command {
    repeated string tokens = 1;
}

message Response {
    string output = 1;
}

service SkyrayService {
    rpc Connect(stream Command) returns (stream Response) {}
}